#include <stdio.h>
#include <math.h>

int main()
{
    int a = sqrt(64);
    int b = ceil(64);
    int c = floor(64);
    int d = round(64);
    int e = abs(-23);
    int f = pow(5, 2);

    return 0;
}