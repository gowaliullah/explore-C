#include <stdio.h>

int main() {
    int a = 10;
    int b = 3;

    // If-Else Example
    if (a > b) {
        printf("A is greater than B\n");
    } else {
        printf("A is not greater than B\n");
    }

    return 0;
}